<%= simple_form_for [@step, @ingredient] do |f| %>
  <% if @ingredient.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@ingredient.errors.count, "error") %> prohibited this ingredient from being saved:</h2>

      <ul>
      <% @ingredient.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  
  <div class="input-group">
		<span class="input-group-addon">@</span>
  			<%= f.association :recipe, :label => false, :default => @step.recipe, input_html: { class: "form-control" } %>
  </div>
  <div class="form">
  	<%= f.input :name, :label => false, input_html: { class: "form-control" }, :placeholder =>"ingredient"%>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
